

console.log(document.body.firstChild);

let a = document.body.firstChild;


console.log(a.parentNode);
console.log(a.parentElement); // returns the parent element of the specified element


console.log(document.body.parentElement); // returns the parent element of the specified element
console.log(document.body.parentNode); // returns the parent node of the specified node

console.log(a.nextSibling.lastsibling);