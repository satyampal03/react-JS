
let string = "";

let buttons = document.querySelector(".button");  // it gives me all nodes with class buttons, like array Not exactly array



Array.from(buttons).forEach((button) => {
    
}) // Array.from(buttons)  this section only converting into real array
