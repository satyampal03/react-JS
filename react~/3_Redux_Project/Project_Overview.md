# Project Setup -- Configure
> In this Project We are going to create a application where we will empliment API's Data --> [Free Images, GIF, Videos], With the Search Feature Also 

## Create React App

- npm create vite ~ Normal
- npm create vite@latest ~ For Latest -V

- npm create vite . ~ Will Create in same Folder It will never ask-about Project-Folder name.

- npm create vite@latest . ~ Will Work on same current folder with latest-Packages.

## Create and Setup the ~ Redux

- npm install @reduxjs/toolkit react-redux ~ if redux with react 

- else

- npm install @reduxjs/toolkit  ~ This is enough for Vanilla js.
